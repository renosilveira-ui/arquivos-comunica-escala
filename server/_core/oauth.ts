import type { Express } from "express";

/**
 * Placeholder for OAuth route registration.
 * Wire up actual OAuth callback routes here when ready.
 */
export function registerOAuthRoutes(_app: Express): void {
  // TODO: implement OAuth routes
}
